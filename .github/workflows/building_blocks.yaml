name: Building blocks

on:
  workflow_dispatch:

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello, World"

  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Test step
        run: |
          echo "This step is working"

      - name: Say goodbye
        run: echo "Goodbye, world"